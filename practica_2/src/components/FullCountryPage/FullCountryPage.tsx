'use client';

import {FullCountry} from "@/types/Country";

type Props = FullCountry;

const paginaPais = (entrada:Props) =>
{
    return(
        <div>
            test
        </div>
    );
}

export default paginaPais;